[build]
command = "npx sanity build -y studio"
publish = "studio/dist"

# Optional: You can add Sanity-specific headers if needed
[[headers]]
for = "/*"
[headers.values]
Cache-Control = "public, max-age=3600"
